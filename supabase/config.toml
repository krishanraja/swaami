# Project ID is now managed via environment variables
# Remove hardcoded project_id to support multiple projects

[functions.transcribe-audio]
verify_jwt = false

[functions.seed-demo-users]
verify_jwt = false
